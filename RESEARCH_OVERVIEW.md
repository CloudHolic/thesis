# VSRG 난이도 측정 연구 개요

## 문서의 목적

이 문서는 osu!mania를 비롯한 수직 스크롤 리듬게임(VSRG)의 채보 난이도를 플레이 응답 데이터로 측정하려는 연구의 문제의식, 핵심 전제, 장기 목표와 단계적 로드맵을 설명한다. 특정 구현의 세부 지시는 `PILOT_IMPLEMENTATION_SPEC.md`에 둔다.

이 연구의 최종 목표는 기존 난이도 숫자를 더 잘 모사하는 예측기를 만드는 것이 아니다. 직접 관측할 수 없는 난이도라는 구성개념(latent construct)을 실제 플레이 결과가 남기는 통계적 흔적으로 측정하고, 장기적으로는 채보 자체에서 그 측정값을 추정할 수 있는 체계를 만드는 것이다.

## 1. 출발점: GT 난이도는 존재하지만 직접 관측할 수 없다

이 연구는 다음 전제를 명시적으로 채택한다.

> VSRG 채보의 난이도를 정확하게 설명하는 ground-truth 난이도는 객관적 잠재 특성으로 존재하지만, 그것을 직접 관측하는 방법은 원리적으로 없다.

여기서 GT는 어딘가에 이미 기록되어 있으나 아직 발견하지 못한 라벨이 아니다. 채보가 플레이어 집단에 가하는 요구를 완전하게 기술하는 잠재량을 뜻한다. 그것을 직접 기술할 수 있었다면 이미 그 체계로 난이도를 정의할 수 있었을 것이고, 각종 난이도 체계에서 반복되는 과대평가·과소평가 논란도 지금과 같은 형태로 남지 않았을 것이다.

따라서 Star Rating(SR), performance points(pp), 제작자 표기, 커뮤니티 평가 등은 GT 자체가 아니다. 이들은 동일한 잠재 구성개념을 서로 다른 규칙과 표본으로 측정하는 **불완전한 측정기**의 출력이다. 각 측정기는 특정 패턴, 곡 길이, 고밀도 구간, 키 수 또는 플레이어 집단에 고유한 편향을 가질 수 있다.

이 관점에서 연구 질문은 “정답 난이도 숫자를 어떻게 예측할 것인가”가 아니라 다음과 같다.

> 직접 관측할 수 없는 난이도라는 구성개념을 플레이어들의 실제 행동 데이터로 어떻게 측정할 것인가?

## 2. 왜 response data와 IRT인가

GT 난이도는 보이지 않지만 그 결과는 관측된다. 플레이어 `p`가 채보 `i`에서 낸 점수 또는 정확도 `y_pi`는 채보가 요구하는 특성과 플레이어 능력의 상호작용으로 생긴다.

```text
잠재적인 채보 특성 + 잠재적인 플레이어 능력 → 관측된 플레이 결과
```

한 번의 플레이는 컨디션, 선곡, 패턴 특화, 운과 재시도 횟수에 크게 흔들린다. 그러나 많은 플레이어와 많은 채보가 연결된 response matrix에는 “어떤 수준의 플레이어들이 이 채보에서 어떤 결과 분포를 보였는가”라는 반복 가능한 통계적 흔적이 남는다.

문항반응이론(IRT)은 이 구조를 위해 만들어진 측정 틀이다. 채보를 문항(item), 플레이어를 응답자(person), 정규화된 accuracy 또는 score를 응답(response)으로 보고, 플레이어 능력과 채보 특성을 분리해 추론한다. 이 연구에서 플레이어 능력은 최종 산출물이 아니라 채보 측정을 위해 통합해 제거할 nuisance latent variable이다.

IRT는 목적 그 자체가 아니다. 목표는 학습 가능한 난이도 측정 체계이며, 현재의 IRT 모형이 데이터 또는 연구 질문에 맞지 않는다는 증거가 쌓이면 다른 접근을 검토할 수 있다. 다만 현재 파일럿에서는 이미 선택한 ZOI Beta-IRT를 정확히 구현하고 검증하는 것이 우선이다.

## 3. 왜 난이도 하나를 바로 학습하지 않는가

처음부터 scalar 난이도를 정의하면 그 정의가 맞다는 것을 전제한 채 모델을 학습하게 된다. 그러나 이 연구에서는 난이도 정의 자체가 아직 검증 대상이다. 먼저 관측된 응답 분포를 설명하는 문항 파라미터를 학습한다.

각 채보 `i`의 문항 파라미터는 다음과 같다.

```text
τ_i = (a_i, b_i, ω_i, γ_0i, γ_1i)
```

- `a`: 능력 변화에 대한 반응의 변별도
- `b`: Beta 연속 성분 평균의 위치를 정하는 절편. 현재 부호 규약에서는 값이 클수록 같은 `θ`에서 평균 성과가 높아지는 **easiness 방향**이다.
- `ω`: Beta 성분의 dispersion parameter
- `γ_0`, `γ_1`: 정확히 0과 정확히 1에 놓이는 확률질량을 정하는 임계값

`τ`는 최종 난이도 점수가 아니다. 이것은 채보가 플레이어 능력과 상호작용하여 어떤 응답 분포를 만드는지를 설명하는 중간 측정 표현이다. 연구는 다음 순서를 따른다.

```text
response data → τ 학습 → 난이도 구성개념 후보 검증
             → chart features → τ 일반화 → 실용 난이도 측정기
```

이 순서를 지켜야 “아직 의미를 검증하지 않은 숫자”를 매우 정확하게 예측하는 모델을 만드는 오류를 피할 수 있다.

## 4. τ에서 난이도를 읽는 일도 연구 질문이다

같은 채보라도 다음 질문은 서로 다른 순위를 만들 수 있다.

- Beta 연속 성분에서 90%, 95%, 98% 평균 성과를 내는 데 필요한 능력은 얼마인가?
- 만점 확률이 50%가 되는 능력은 얼마인가?
- 같은 능력의 플레이어 사이에서 결과가 얼마나 안정적으로 모이는가?

따라서 파일럿에서는 `b*(0.90)`, `b*(0.95)`, `b*(0.98)`, `γ_1/a`, `γ_0/a`, `ω`를 함께 살핀다. 이 가운데 `b*(y)`는 ZOI 전체 기대값이 아니라 **Beta 연속 성분의 조건부 평균**이 `y`가 되는 능력이다.

난이도 후보들이 대체로 일관되면서도 특정 채보에서 해석 가능한 차이를 보인다면, 단일 숫자보다 풍부한 난이도 구조가 있다는 증거가 된다. 반대로 차이가 최적화 불안정이나 비식별성에서 나온다면 측정 모형을 고쳐야 한다. “난이도 하나로 완전히 요약되지 않는 채보가 존재한다”는 결론도 실패가 아니라 가능한 연구 결과다.

## 5. SR과 pp는 왜 target이나 feature가 아닌가

SR 또는 pp를 학습 target으로 쓰면 연구 질문은 기존 측정기의 모사가 된다. feature나 사전학습 표현으로 넣어도 기존 규칙 기반 편향이 모델 안으로 재유입된다. 특히 기존 체계가 놓치는 곡 길이 또는 패턴 효과를 발견하려는 연구에서 이는 순환 논증을 만든다.

그러므로 SR과 pp는 다음 용도로만 쓴다.

- 학습이 끝난 뒤 새 측정값과 비교하는 external comparator
- 기존 측정기와 어디서 일치하고 어디서 체계적으로 갈리는지 조사하는 related-work baseline

SR과 높은 상관은 최소한의 수렴 타당성 증거가 될 수 있지만, 상관 1은 목표가 아니다. 낮은 상관 역시 자동으로 혁신의 증거가 아니다. 홀드아웃 likelihood, calibration, rate 변형의 순서, 난이도 후보 간 관계와 실제 채보 사례를 함께 보아야 한다.

## 6. 방법론적 철학

### 6.1 측정과 예측을 분리한다

먼저 response model이 문항 특성을 회복할 수 있는지 확인하고, 그다음 단순 feature encoder가 그 특성을 채보에서 일반화할 수 있는지 본다. 측정 실패와 표현 학습 실패를 한 번에 섞지 않는다.

### 6.2 자유 문항 모형을 기준선으로 둔다

ID encoder는 각 채보에 사실상 자유로운 `τ_i`를 부여한다. 이는 현재 likelihood와 데이터가 허용하는 적합도의 상한이다. feature encoder와의 차이는 단순히 모델 성능 격차가 아니라, 선택한 채보 feature가 response-derived 문항 구조를 얼마나 설명하는지 보여준다.

### 6.3 합성 recovery를 실데이터보다 먼저 한다

알려진 `τ`로 데이터를 생성하고 다시 추정해 회복 여부를 본다. 여기서 실패하면 실제 데이터의 편향이나 feature 부족이 아니라 kernel, 구적법, 파라미터화 또는 최적화 구현을 의심해야 한다. 이는 구현 오류와 model–data mismatch를 분리하는 기계적 검증 단계다.

### 6.4 불확실성과 비식별성을 숨기지 않는다

응답이 얇거나 경계 관측이 부족한 문항에서는 일부 파라미터가 약하게 식별될 수 있다. MAP 점추정과 full-covariance 변분 추론을 함께 검토하는 이유는 평균값뿐 아니라 이러한 불확실성과 파라미터 간 능선을 관찰하기 위해서다.

### 6.5 데이터 선택 구조를 측정 문제의 일부로 본다

플레이어는 무작위로 채보를 플레이하지 않는다. 자기 실력과 취향에 따라 선곡하므로 결측은 MNAR일 수 있다. 최고 기록만 남기는 중복 제거도 순서통계량 편향을 만든다. 인기 채보 위주의 두꺼운 표본은 모집단 대표성을 보장하지 않는다. 이 한계들은 데이터 정제만으로 사라진다고 가정하지 않고 결과 해석과 후속 연구에 남긴다.

### 6.6 서로 다른 키모드의 척도 연결을 먼저 확인한다

4K와 7K에는 공유 문항이 없으므로 두 모드를 모두 플레이한 사용자가 공통 척도를 연결한다. 충분한 linking person이 없다면 두 모드의 숫자는 각각 다른 모집단 기준으로 표준화된 값에 불과하다. 이 경우 모드 간 절대 비교를 포기하고 모드 내 검증으로 제한해야 한다.

## 7. 장기 목표

장기적으로는 다음 기능을 갖춘 측정 체계를 지향한다.

1. 플레이 응답으로부터 채보의 다차원 문항 특성과 불확실성을 추론한다.
2. 어떤 `τ` 기반 함수가 VSRG에서 유효한 난이도 구성개념인지 경험적으로 검증한다.
3. 단순 feature를 넘어 채보 이벤트 또는 시간×컬럼 표현을 직접 인코딩하여, 플레이 기록이 적거나 없는 새 채보에도 `τ`를 일반화한다.
4. 여러 키모드와 rate 변형을 연결된 척도에서 다룬다.
5. 기존 SR/pp와의 차이가 어떤 패턴과 플레이 경험에서 발생하는지 설명 가능한 형태로 제시한다.
6. 단일 난이도 숫자가 충분하지 않은 경우 난이도, 고득점 난도, 퍼펙트 난도, 결과 일관성 등의 복수 축을 제공한다.

## 8. 단계적 연구 로드맵

### 단계 0: 데이터 가능성 확인

- random pool에서 4K–7K linking user 수를 측정한다.
- 양방향 k-core 곡선으로 문항 수와 응답 밀도의 가능한 균형을 확인한다.
- rate-group pair, 키모드 분포, 응답 경계 질량을 조사한다.

### 단계 1: 확률 모형의 기계적 검증

- ZOI Beta-IRT kernel과 `θ ~ Normal(0,1)` Gauss-Hermite 주변화를 구현한다.
- 합성 데이터에서 `τ` recovery와 `Q` 민감도를 검증한다.
- 수치 안정성과 경계 분기 구현을 검사한다.

### 단계 2: 실데이터 자유 문항 기준선

- ID encoder로 accuracy와 score를 각각 fit한다.
- `τ`의 식별성, posterior predictive calibration, 홀드아웃 likelihood를 확인한다.
- 복수 난이도 후보와 rate-group 순서, SR 비교, 후보 간 순위 역전 사례를 분석한다.

### 단계 3: 단순 채보 feature로 일반화

- linear encoder로 파이프라인과 계수 방향을 점검한다.
- 얕은 MLP부터 시작해 ID 상한과의 격차를 측정한다.
- 가능하면 item holdout으로 새 채보 일반화 능력을 검증한다.

### 단계 4: 채보 직접 인코딩

- 시간×컬럼 격자 또는 이벤트 토큰화를 비교한다.
- 가변 키 수와 가변 길이를 보존하고 rate를 입력 시간축에 적용한다.
- 단순 feature 단계에서 검증된 동일한 `τ` 인터페이스와 평가 체계를 유지한다.

### 단계 5: 측정 모형의 확장과 편향 분석

- latent population distribution 가정에 대한 민감도를 점검한다.
- MNAR, 재시도 횟수와 최고 기록 순서통계량을 명시적으로 모델링할 방법을 연구한다.
- 필요할 때 one-inflated-only 모형, 더 유연한 사후분포 또는 다차원 능력 모형을 비교한다.

## 9. 성공의 의미

파일럿의 성공은 새로운 단일 난이도 숫자가 즉시 완성되는 것이 아니다. 다음 연쇄가 성립하는 것이 성공이다.

1. 합성 데이터에서 알려진 문항 파라미터를 회복한다.
2. 실데이터에서 자유 문항 모형이 안정적이고 예측적으로 타당한 response structure를 학습한다.
3. 그 구조에서 파생한 난이도 후보들이 VSRG 도메인에서 해석 가능한 관계와 차이를 보인다.
4. 단순 chart features가 그 구조의 일부를 재현하며, 이후 직접 인코더 연구의 기준선을 제공한다.

이 연구는 GT를 관측했다고 주장하지 않는다. 관측 불가능한 GT 난이도가 존재한다는 전제 아래, 플레이 행동이 주는 통계적 제약을 이용해 기존 측정기와 독립적인 새로운 측정 체계를 구축하고 검증하려는 시도다.
